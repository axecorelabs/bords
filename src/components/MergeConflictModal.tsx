'use client'

/**
 * MergeConflictModal — DEPRECATED
 * Merge conflicts are now handled by Y.Doc CRDT resolution.
 * This component is kept as a no-op stub for safety.
 */
export function MergeConflictModal() {
  return null
}
